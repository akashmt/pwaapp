input[type=checkbox] {
  position: fixed;
  left: -9999px;
  opacity: 0;
  &:checked ~ {
    .nav-toggle span {
      &:nth-of-type(1) { top: 12px; transform: rotate(45deg); }
      &:nth-of-type(2) { display: none; }
      &:nth-of-type(3) { top: 12px; margin-top: -2px; transform: rotate(-45deg); }
    }
    .offcanvas-menu { margin-right: 0px; }
    .offcanvas-main { margin-left: -250px; }
  }
}